import { ModListService } from './mod-list.service';

describe('ModListService', () => {
	describe('init', () => {
		it('Should be defined', () => {
			expect(ModListService).toBeDefined();
		});
	});
});
